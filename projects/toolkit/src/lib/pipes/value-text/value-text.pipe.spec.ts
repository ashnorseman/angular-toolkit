import { ValueTextPipe } from './value-text.pipe';

describe('ValueTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueTextPipe();
    expect(pipe).toBeTruthy();
  });
});
